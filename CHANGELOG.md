# @refyne/eslint-plugin

## 0.2.0

### Minor Changes

-   Add new `dto-validation` ESLint rule to validate DTO types and enforce consistent DTO declarations.

    This change introduces the rule implementation in `src/rules/dto-validation.rule.ts` and associated tests.
